# Ambiente — NUNCA versionar
.env
.env.local
.env.production

# Configurações locais
.claude/
config/config.local.php

# Backups gerados
backups/*.sql
backups/*.sql.gz

# Logs
*.log
*.cache

# OS
.DS_Store
Thumbs.db

# Uploads de usuário (se existir)
assets/uploads/

# Dependências (se usar Composer no futuro)
vendor/
